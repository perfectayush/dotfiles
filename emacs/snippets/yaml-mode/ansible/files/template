# -*- mode: snippet -*-
# name : Templates a file out to a remote server.
# key : template
# condition: ansible
# --
- name: ${1:Templates a file out to a remote server.}
  template: src=$2 dest=$3 mode=${4:0644} owner=${5:root} group=${6:root} $0
